# Phase 1: TrafficExtension with inline Lua — input guardrail (no Wasm required)
#
# Targets pods labeled: guardrails.trustyai.io/config=pii
# Phase: AUTHZ (runs before routing — ideal for blocking)
#
# Confirmed working on: OSSM 3.4.1 / Istio 1.30.3 / OCP 4.20 (2026-08-12)
# Phase 1 test results:
#   Clean request      → 200 ✅
#   Email PII          → 403 ✅ (blocked_by_guardrail)
#   Phone PII          → 403 ✅ (blocked_by_guardrail)
#   api_key (regex)    → 403 ✅ (blocked_by_guardrail)
#
# BEFORE APPLYING: verify NEMO_CLUSTER value matches your cluster.
# Run: oc exec -n app-ns <pod> -c istio-proxy -- \
#        pilot-agent request GET /clusters | grep nemo-pii
# Expected: outbound|80||nemo-pii.guardrails.svc.cluster.local
#   (TrustyAI operator exposes NeMo on ClusterIP port 80 → targetPort 8000)
apiVersion: extensions.istio.io/v1alpha1
kind: TrafficExtension
metadata:
  name: nemo-input-guard
  namespace: app-ns
spec:
  selector:
    matchLabels:
      guardrails.trustyai.io/config: pii
  phase: AUTHZ
  priority: 10
  lua:
    inlineCode: |
      -- Port 80: TrustyAI operator service port (ClusterIP 80 → pod 8000)
      -- Timeout 3000ms: NeMo cold-start ~1.2s; 300ms caused false 504s
      local NEMO_CLUSTER = "outbound|80||nemo-pii.guardrails.svc.cluster.local"
      local NEMO_HOST    = "nemo-pii.guardrails.svc.cluster.local"
      local NEMO_PATH    = "/v1/guardrail/checks"
      local TIMEOUT_MS   = 3000

      function envoy_on_request(request_handle)
        local body = request_handle:body(true)
        if body == nil or body:length() == 0 then return end

        local ok, resp_headers, resp_body = pcall(function()
          return request_handle:httpCall(
            NEMO_CLUSTER,
            {
              [":method"]      = "POST",
              [":path"]        = NEMO_PATH,
              [":authority"]   = NEMO_HOST,
              ["content-type"] = "application/json",
            },
            body:getBytes(0, body:length()),
            TIMEOUT_MS
          )
        end)

        if not ok then
          request_handle:respond(
            {[":status"] = "403", ["content-type"] = "application/json"},
            '{"error":"guardrail_unavailable","message":"Guardrail service unreachable"}'
          )
          return
        end

        local http_status = resp_headers[":status"]
        if http_status ~= "200" then
          request_handle:respond(
            {[":status"] = "403", ["content-type"] = "application/json"},
            string.format('{"error":"guardrail_error","message":"NeMo returned HTTP %s"}', http_status)
          )
          return
        end

        if string.find(resp_body, '"status"%s*:%s*"blocked"') then
          request_handle:respond(
            {[":status"] = "403", ["content-type"] = "application/json"},
            '{"error":"blocked_by_guardrail","message":"Request blocked by safety guardrails"}'
          )
        end
      end
